export * from './src';

export {
  memoize,
  cloneDeep,
  omit,
  upperFirst,
  // 去重
  uniq,
  // 自定义去重
  uniqBy,
  // pick,
  merge,
  // 浅拷贝
  // clone,
  set,
  get,
  // fromPairs,
  // difference,
  // intersection,
} from 'lodash-es';
